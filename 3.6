//Задание 3.6
//Ваши имя и фамилия: Hanna Skoropadska

//1. Составьте команды, в которых:

//   переменной а присваивается значение null;
let a = null;

//   выводится значение переменной a;
console.log(a);

//   выводится тип переменной a;
console.log(typeof a);

//   выводится результат сравнения переменной a с нулем; 
console.log(a > 0);

//   выводится результат сравнения переменной a со значением null; 
console.log(a === null);

//   значение переменной a удваивается;
console.log(a * 2);

//   переменная увеличивается на 1;
console.log(a + 1);

//2. Запишите последовательность команд для решения задачи. Используйте 
// "говорящие" имена переменных. В каждом углу комнаты сидело по кошке. 
//  У каждой кошки х котят. Сколько всего ног? Значение x задайте произвольное.
let mamaCats = 4;
let kittensX = 5;
let legCount = mamaCats * 4 + (kittensX * 4);
console.log(legCount);

// Выполните программу для значения x = 5 (количество котят у кошки).
// А теперь исправьте количество котят у кошки на значение null и выполните программу.
// Какой ответ получится?
{
  let mamaCats = 4;
  let kittensX = null;
  let legCount = mamaCats * 4 + (kittensX * 4);
  console.log(legCount);
}

//Answer: programm counts only mamaCats
